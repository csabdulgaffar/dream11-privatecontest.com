{
  "name": "private-contest.comn",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix ./client && npm run build --prefix ./client && npm install --prefix ./server",
    "start": "cd server && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
